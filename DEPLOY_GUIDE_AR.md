# دليل رفع موقع "كوافير" (Kwafer) مجاناً 🚀

لرفع الموقع مجاناً للتجربة، سنستخدم منصة **Render** لأنها تدعم Node.js وقاعدة بيانات PostgreSQL.

## الخطوة 1: تجهيز حساباتك
1. اشترك في موقع [GitHub](https://github.com) إذا لم يكن لديك حساب.
2. اشترك في موقع [Render](https://render.com) واربطه بحساب GitHub الخاص بك.

## الخطوة 2: رفع الكود على GitHub
1. قم بإنشاء مستودع (Repository) جديد على GitHub باسم `kwafer-project`.
2. ارفع ملفات مشروعك (ما عدا مجلد `node_modules`) إلى المستودع.
   - *نصيحة: تأكد من وجود ملف `.gitignore` يحتوي على `node_modules` و `.env`.*

## الخطوة 3: إنشاء قاعدة البيانات على Render
1. في لوحة تحكم Render، اضغط على **New** ثم اختر **PostgreSQL**.
2. اسم القاعدة: `kwafer_db`.
3. اختر الخطة المجانية (**Free**).
4. بعد الإنشاء، انسخ رابط الـ **Internal Database URL** (ستحتاجه لاحقاً).

## الخطوة 4: إنشاء خدمة الويب (Web Service)
1. في Render، اضغط على **New** ثم اختر **Web Service**.
2. اربط المستودع الذي أنشأته على GitHub.
3. الإعدادات:
   - **Environment:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
4. اختر الخطة المجانية (**Free**).

## الخطوة 5: إضافة المتغيرات البيئية (Environment Variables)
في إعدادات الخدمة (Environment)، أضف المتغيرات التالية:
*   `NODE_ENV`: `production`
*   `DATABASE_URL`: (الصق هنا الرابط الذي نسخته من خطوة قاعدة البيانات)
*   `JWT_SECRET`: (أي كلمة سر قوية من اختيارك)
*   `PORT`: `10000` (أو اتركه وسيقوم Render بتحديده)

## الخطوة 6: تشغيل الجداول (Migration)
لأن قاعدة البيانات فارغة، ستحتاج لتشغيل ملف الـ `schema.sql`.
*   يمكنك استخدام أداة (Shell) داخل Render لفتح `psql` وتنفيذ محتويات الملف، أو استخدام تطبيق مثل **DBeaver** للاتصال بالقاعدة عن بُعد وتنفيذه.

---
**مبروك!** سيقوم Render ببناء الموقع وسيعطيك رابطاً (مثلاً: `kwafer.onrender.com`) يمكنك فتحه وتجربته. ✨
